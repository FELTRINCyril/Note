# Sous-agents

Ces fichiers définissent les sous-agents spécialisés que Claude Code utilisera (voir `CLAUDE.md` §3).

**À faire une seule fois** : copie ces 5 fichiers dans `.claude/agents/` à la racine du projet
(je n'ai pas pu écrire dans `.claude/` directement depuis cette session — c'est un dossier protégé).

Tu peux simplement demander à Claude Code au démarrage :
> « Copie les fichiers de `sous-agents/` vers `.claude/agents/`. »

Agents fournis : `data-modeler`, `swiftui-builder`, `editor-specialist`, `design-integrator`, `swift-reviewer`.
